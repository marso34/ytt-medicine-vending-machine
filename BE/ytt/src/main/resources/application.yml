spring:
  application:
    name: ytt

  profiles:
    active: prod
    include: common, secret

management:
  endpoints:
    web:
      exposure:
        include: health, info

    jmx:
      exposure:
        exclude: '*'

  endpoint:
    health:
      show-details: always
